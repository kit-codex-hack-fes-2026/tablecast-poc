# TableCast 共通デザイン

Web・スライド・動画で共有する見た目の基準。2026-09-14版。

[画像付きリファレンス](docs/design/README.md) · [画面と操作の仕様](docs/ui.md)

参考画像は雰囲気・書体・配色・余白・質感の見本である。画面の区切り、配置、機能、文言、レスポンシブ挙動の実装仕様ではない。実UIの契約は `docs/ui.md` と現行実装を参照する。資料制作の契約は [Issue #1](https://github.com/kit-codex-hack-fes-2026/tablecast-poc/issues/1) を参照する。

## 基本方針

会話に集中できる白黒のUIを土台に、Agent機能の存在を虹色で示す。親しみやすい日本のSaaSとして、ゆとり、整った文字組み、明瞭な操作を優先する。CyberAgentのような人に近いコミュニケーションを方向の参考にし、ブランド色やロゴを模倣しない。白い面、適度に曇ったガラス、柔らかなサテンの反射を組み合わせる。極端なマットや粉っぽい表面にはせず、薄いハイライトで質感を残す。クローム、鏡面、強い反射、宝石のようなオーブ、大きな光の筋は避ける。

今回の画像は画像生成による雰囲気の探索であり、実装キャプチャの取得・差し込みを制作要件にしない。現行の会話注文UIのまとまり、オーブと横長の波形を見た目の参照にし、架空の操作や別レイアウトの提案へ広げない。生成画像を実装の証拠として扱わない。将来プレゼンやデモ本体を制作する際のUI部分は、現行実装の画面・録画に置き換える。以下は推奨基準であり、アプリへの反映済みを意味しない。

## 今回の参照画像の範囲

現行の画像成果物は、会話UI・汎用部品・構成図・利用場面の写真をまとめた統合リファレンス1枚とTableCastロゴ。色・角丸・フォントを個別画像へ展開せず、本Markdownに定義する。画像中にデザイン数値を記載しない。旧案、比較画像、試作アーカイブは配布しない。

画像は見た目の参考であり、製品の画面仕様や実装済み機能を示すものではない。正確な色・書体・ウェイトは本Markdownを正本にする。

将来、実際の製品画面を資料に載せる場合は次の境界を守る。

- 来店客はログインせず、会話に顔写真・アバター・プロフィール設定を追加しない。現行の役割ラベルと横長波形を保つ。
- 店舗向けAIは外部ChatGPTのAgent Pluginで使う。TableCast独自の店舗AIチャットや対応スタッフ割当を追加しない。
- 画面のレイアウトは実装を根拠にする。ダミーデータを入れるために製品機能を増やさない。

## 書体と文字階層

日本語は **Noto Sans CJK JP**、欧文・数字は **Inter**。混植のCSS順は `Inter, "Noto Sans CJK JP", sans-serif`。日本語の文章中でも欧文と数字をInterにする。日本語版CJKの字形を使い、Noto Sans JPやOS標準への代替を同一フォントとして報告しない。

| 用途                                   | Tailwind      | weight |
| -------------------------------------- | ------------- | ------ |
| 本文、説明、補足、通常ラベル           | `font-normal` | 400    |
| 見出し、主要操作、重要な数値、選択状態 | `font-bold`   | 700    |

500・600・細字は共通トークンに持たせない。階層はサイズ、配置、余白で作る。`font-synthesis: none` と実フォントの400・700を用い、疑似太字にしない。日本語本文の字間は通常、行高は1.5〜1.75、見出しは1.25〜1.4を基本にする。

| 媒体・役割              | 推奨値              | 対応・運用                                                   |
| ----------------------- | ------------------- | ------------------------------------------------------------ |
| Webの補足               | 14px / 400          | `text-sm font-normal`。主操作や必要な説明を小さくしない      |
| Webの本文               | 16px / 400          | `text-base font-normal leading-relaxed`                      |
| Webの読みやすい会話文   | 18px / 400          | `text-lg font-normal leading-relaxed`                        |
| Webの見出し             | 24〜32px / 700      | `text-2xl`、`text-3xl`を優先。現行の32px未満の標準値でもよい |
| 1920×1080資料の主見出し | 64px / 700          | 結論を短い一文で置く                                         |
| 資料の本文・字幕        | 32px / 400、強調700 | 字幕は原則2行まで。収まらなければ場面を分ける                |
| 資料の脚注・章名        | 24px / 400、章名700 | 読むべき制約を脚注だけに隠さない                             |

上表は制作原本の寸法であり、WebのCSS pxと投影時の見え方を同一視しない。資料の縮小・投影条件で確認し、重要な文章を縮めて詰め込まない。コード断片も基本2書体を維持し、空白揃えが必須なコードは実エディターのキャプチャを根拠として使う。

## 配色

| 意味                   | 色                         | 現行の対応                                   |
| ---------------------- | -------------------------- | -------------------------------------------- |
| 全体背景               | `#fafafa`                  | `bg-background`                              |
| 情報を載せる面         | `#ffffff`                  | `bg-card`                                    |
| 本文・主要操作         | `#18181b`                  | `text-foreground`、`bg-primary`              |
| 主要操作上の文字       | `#ffffff`                  | `text-primary-foreground`                    |
| 補助面                 | `#f4f4f5`                  | `bg-secondary`、`bg-muted`                   |
| 補助文字               | `#3f3f46`                  | `text-muted-foreground`                      |
| 装飾的な境界           | `#e4e4e7`                  | `border-border`                              |
| 操作の識別に必要な境界 | `#71717a`                  | `border-input`                               |
| 成功                   | `#507137` / 淡色 `#f0f5e8` | `text-success`、`bg-success-soft`            |
| エラー・破壊操作       | `#ad3429`                  | `text-destructive`                           |
| 警告                   | `#92400e` / 淡色 `#fffbeb` | 資料用の状態色。アプリの既存トークンではない |

虹色の原本は `apps/web/src/styles.css` の `--voice-spectrum`。色順は `#6366f1 → #a855f7 → #ec4899 → #fb923c → #facc15 → #2dd4bf → #38bdf8 → #6366f1`。虹色の意味はAgent機能に限定する。Agentの音声入出力、応答生成、提案など、その機能を示す要素にだけ使う。単なる注目、一般的な音声、装飾、材質見本、人物の服には使わない。色見本にもAgent機能と明記する。本文、成功、エラーの意味は別に保ち、虹色だけで状態を区別しない。採用したTableCastロゴは単色とし、虹色を加えない。

淡い境界線は装飾用途であり、入力欄やフォーカスを識別する唯一の手段にしない。通常文字4.5:1以上、大きな文字3:1以上、必要な操作境界や図の線は3:1以上を基準にする。透明面は合成後の最も読みにくい背景で確認する。

## 音声波形とorbの形状

`apps/web/src/features/kiosk/audio-waveform.tsx`の形状を継承する。変更できるのは色と影。形状・構成・比率を参考画像の都合で作り直さない。

- 波形は横長の細い連続線。独立した棒や点、面で塗りつぶした波へ変更しない。
- orbは白い円形面にSmokeRingを重ね、中央に小さな状態アイコンを置く。球体、顔、大きなドーナツ状の立体へ変更しない。
- orbを左、ラベルと横長波形を右、音声操作を下に置く。写真内の端末にも同じ関係を使う。
- 基準寸法と参照した実装は[統合画像の制作記録](docs/design/source/UNIFIED.md)に記載する。画像は静止した見た目の参考で、実装の録画やピクセル一致の証明にはしない。

## 余白・角丸・面

余白は4px単位。`gap-2/4/6/8/12/16`、`p-2/4/6/8/12/16`に対応する8・16・24・32・48・64pxを中心にする。別の値を必要とする場合も標準utilityを優先する。

小さな情報群の内側は8〜16px、カードの内側は16〜24px、内容群の間は24〜32px、節の間は48〜64pxを出発点にする。資料の外周は1920×1080で64px以上を取り、下端には脚注用の領域を確保する。

### 角丸

現行の `--radius: 0.625rem` を継承する。以下のpx値はルート16px時の値。Tailwindの素の既定値ではなく、このリポジトリのカスタム定義である。

| 用途                               | utility        | 値                                 |
| ---------------------------------- | -------------- | ---------------------------------- |
| 小さなタグ、補助表示               | `rounded-sm`   | `calc(var(--radius) - 4px)` = 6px  |
| ボタン、入力、基本カード           | `rounded-lg`   | `var(--radius)` = 10px             |
| 大きなカード、ガラス面、グループ枠 | `rounded-xl`   | `calc(var(--radius) + 4px)` = 14px |
| 音声オーブ、状態表示               | `rounded-full` | 円・カプセル                       |

`rounded-md`などの既存コードを今回削除・変更するわけではない。新しい見本では上の4種類に絞る。資料では部品と角丸を同じ倍率で拡大する。枠と内側は幾何学的に揃え、外周半径から枠厚を引く。Agent機能を示す虹色の枠を使う場合も、内外の曲率を揃える。派生計算は新しい角丸トークンとして増やさない。

### 質感とシャドー

| 面     | 見せ方               | 見本の値                        |
| ------ | -------------------- | ------------------------------- |
| 通常面 | 白い面と浅い影       | `0 2px 8px rgb(24 24 27 / 3%)`  |
| 浮遊面 | 半透明面と柔らかな影 | `0 8px 24px rgb(24 24 27 / 6%)` |

この値は資料の出発点。Webは既存の`shadow-xs` / `shadow-sm`を通常面、`shadow-lg`を浮遊面の候補にし、すべてを任意値へ置換しない。最終サイズで見え方を確認する。

- Frosted glass: 背後の形がぼんやり透ける曇りと、縁に残る柔らかな反射を両立する。不透明度を一律に固定して白い塊にしない。文字の領域は白を基本にする。
- Soft sheen / Satin metal: 広く弱いハイライトで穏やかな光沢を残す。粉っぽい陶器や粗い石の質感、鏡面クロームは避ける。材質見本に虹色を入れない。ロゴはフラットな形を正本にする。
- Neumorphism: 左上の白いハイライトと右下の灰色の柔らかな影を対にし、白から薄灰への微細なグラデーションで浅い凸面を作る。入力欄、スライダートラック、選択中のナビゲーションは浅い内側の影で凹面にする。通常サイズでも凹凸が読み取れる強さを確保し、単なる白いカードのドロップシャドーに戻さない。
- グラデーションと影の強さ: 部品構成の参照では、カードと操作面の白から薄灰の傾斜、明暗一対の影、入力面の内側の影を従来より明確に出す。すべての面を一様な白と単一の影だけで処理しない。文字領域は色むらを抑え、黒い操作の文字は白で明瞭に保つ。
- 適用範囲: セカンダリーボタン、トグル、入力、選択面などの操作部品を中心に使う。テーブルの本文や長文の読み取り面は平坦で明瞭に保つ。黒い主要ボタンは暗色の弱い傾斜と縁の細い反射に留める。
- 通常面・浮遊面の2段階は維持する。凸と凹は操作状態の表現であり、深い彫り、膨らみすぎる角丸、部品ごとの独自シャドーを増やさない。状態は文字・アイコン・境界でも識別し、凹凸や薄い影だけに依存させない。

## Lucideと色による分類

Lucideは操作・状態・利用者・一般概念に使う。既存`lucide-react`の線幅2を基本とし、Webでは16・20・24px。図解は同じ比率で拡大する。塗りアイコンと線アイコンを混ぜて状態差を増やさない。

| 意味             | Lucide           |
| ---------------- | ---------------- |
| 来店客           | `Users`          |
| 店舗スタッフ     | `UserRound`      |
| 音声入力・停止   | `Mic` / `MicOff` |
| 音声処理         | `AudioLines`     |
| 注文かご         | `ShoppingCart`   |
| スタッフ呼び出し | `Bell`           |
| 確認・成功       | `Check`          |
| 注意・エラー     | `CircleAlert`    |
| 言語             | `Languages`      |

UIで文字ラベルがある装飾アイコンは読み上げ対象を重複させない。アイコン単独の操作はアクセス可能な名前を付ける。図では利用者、操作、状態に必ず文字を添える。

| 役割                  | 前景 / 淡い背景       | 運用                       |
| --------------------- | --------------------- | -------------------------- |
| 利用者                | `#3f3f46` / `#f4f4f5` | 人物アイコンとラベル       |
| Web・画面             | `#1d4ed8` / `#eff6ff` | グループ名、細い線、淡い面 |
| API・業務処理・永続化 | `#6d28d9` / `#f5f3ff` | グループ名、細い線、淡い面 |
| 音声経路              | `#0f766e` / `#f0fdfa` | グループ名、細い線、淡い面 |

### ビジュアルコーディング

色は識別したい意味から決める。同じ意味には同じ色・アイコン・ラベルを使い、見栄えの変化のために色を増やさない。意味を短い言葉で説明できない色は無彩色に戻す。

- 役割はLucideと明記した名前で識別する。来店客とスタッフはどちらも人なので灰色を共有し、`Users / 来店客`と`UserRound / 店舗スタッフ`で分ける。Agentは`AudioLines / 接客Agent`と必要な箇所の虹色で示す。
- 図の分類色は該当ノードの見出し線・小さなマーカーに限定し、本文とカード背景は無彩色を基本にする。図中に使った意味だけの凡例を置く。
- 実行境界と機能分類は別にする。WebとAPIを含むCloudflareの外枠は無彩色。Web Workerだけを青、API WorkerとD1を同じ紫で示す。外部ロゴの元の色はブランド識別であり、この分類色には数えない。
- 番号と矢印で順番・因果を示す。ストーリーボードの1枚目・2枚目・3枚目を別色に塗り分けない。すべて同じ白い面に揃える。
- 成功・警告・エラーの状態色は実際にその状態を伝えるときだけ使う。発話中を飾るための緑や、目立たせるだけの虹色を追加しない。
- 写真の自然な色やカラーグレーディングは図解の色コードとは別物。衣服・照明・食器を役割色へ染めない。

上の分類色は資料の共通コード。アプリへ新しい状態色を追加したことを意味しない。状態色とは役割を分け、凡例・ラベル・線種を併用する。外部ブランドのロゴ自体を分類色に塗り替えない。

## 外部ロゴとアーキテクチャ図

外部ロゴは公式SVGを第一選択、公式PNGを第二選択にする。公式サイト、公式リポジトリ、ブランド配布ページを出典とし、取得URLとハッシュを記録する。サービス固有画像がない場合は公式企業ロゴと正確なサービス名を組み合わせる。ロゴを記憶から生成・模写せず、形・縦横比・色を保持する。

代表画像で参照したCloudflare・TanStack・Mastra・D1の公式ロゴは、[出典URLと取得時のハッシュ](docs/design/source/assets.json)を保存し、SVG自体は同梱しない。各ブランドの権利は権利者に帰属する。提携や承認を示す並列ロックアップとして扱わず、図中で採用技術を識別する目的で用いる。

アーキテクチャ図では次を必須とする。

1. 来店客・店舗スタッフなどの利用者とラベル。
2. 採用技術の公式ロゴ、サービス名、短い責務。
3. ブラウザー、Cloudflare、音声runtime、外部サービスなどの名前付きグループ。色の役割分類と実行境界を混同しない。
4. 全矢印の向きと、流れる内容のラベル。`注文API`、`状態通知`、`音声`、`生成テキスト`などを付ける。
5. 同期の要求・応答は実線、非同期の通知は破線、双方向メディアは両端矢印。必ず凡例も付ける。
6. 実装と一致する接続。Web/APIの2 Workers、API内のMastra、Python音声Agent、業務の正本D1を保つ。Mastraを独立サーバーとして描かない。

図を小さくするために接続を変えない。経路を省略した参考図は、対象と省略範囲を図中または脚注へ記載する。詳細な構成は`docs/architecture.md`と実コードで再確認する。

## 媒体別の使い方

今回作るものはUIデザインシステムの見本であり、広告やブランディング資料ではない。キャッチコピー、利用者の声、実測のない効果数値、写真主体の価値提案を追加しない。スライド・動画の見本も実画面の説明と部品の配置を題材にする。

| 場面       | 見せ方                                                       |
| ---------- | ------------------------------------------------------------ |
| 画面説明   | 会話注文または店舗フロアを大きく配置し、画面名を見出しにする |
| 操作の確認 | 同じ画面の対象部位を拡大し、短い注釈を添える                 |
| 技術説明   | 利用者、端末、公式ロゴ、名前付きグループ、ラベル付き通信経路 |
| デモ動画   | UI領域、章名、字幕、脚注を別の領域にする                     |
| 章転換     | 画面名または章名と単色ロゴを簡潔に配置する                   |

今回の配布画像は会話UI・部品・写真・構成図をまとめた1枚に限定する。以下の媒体別方針は将来制作時の運用基準であり、追加画像の制作指示ではない。カラーコード・ウェイト・角丸寸法などのデザイン数値注記と単独の図形見本は載せない。UI内の商品価格などはサンプルとして表示してよい。

章名は左上、主見出しはその場面の結論、脚注は下端の専用領域。字幕はその上に独立した帯を置き、映像の重要な操作を隠さない。字幕は音声がない操作区間にも目的・状態・結果を示す。

動きは順序・因果・注目箇所の説明に使う。Webの小さな反応は既存180ms程度、資料の章移動・フェードは240〜360ms程度を出発点にする。常時回転、強いズーム、文字の反射アニメーションは避ける。Webはreduced-motionで静止、資料は静止画でも意味が通る構成にする。本成果物は静止見本であり、動画の動作検証済みを意味しない。

ストーリーボードや大きな利用例は原則として実写写真にする。自然な肌色、暖かい実光源、穏やかなアンバーのハイライト、柔らかなコントラスト、細かな35mmフィルムグレインを基本にする。強いオレンジ・ティール、演技過剰な広告写真、CGのような肌を避ける。用意する写真調の生成サンプルは実写調の生成画像と明記し、実際に撮影した写真や導入店舗の証拠と混同しない。今回の雰囲気リファレンスでは端末の画面も画像生成で描き、空欄にしない。現行UIのまとまりと横長の波形を参照し、生成画面を新機能の仕様や実装の証拠にはしない。プレゼン・デモ本体の制作時に実装の画面へ置き換える。

ストーリーボードは各コマに役割・状況・行動を示す。写真の表情だけに意味を委ねず、見出しに誰が何をするか、短い説明に前提となる状況を置く。視線、手、操作対象の画面を同時に見せ、何をしているかが静止画だけで分かる構図にする。店舗・卓・注文内容はコマ間で揃え、依頼→確認と明示承認→スタッフの受注が追えるようにする。

写真は会話する人、控えめな手の動き、卓上端末の関係へ視線を導く。肩越しのフレーミング、対角線、前後の層、余白で場面の意味を伝える。明るい窓・照明、料理や酒器、無関係な人物を主役より強調しない。フィルムグレインは写真にのみ適用し、UIや文字には重ねない。

スライド脇の小さな挿絵は白黒の線画とハーフトーンを基本にする。虹色は描かれたAgent機能の箇所にだけ小さく添える。Agentが登場しない挿絵は白黒だけにし、服やトレーへ装飾として差し込まない。白黒ハーフトーンは補助的な挿絵に限り、大きな利用場面の写真を置き換えない。人物・衣服・背景全体をカラフルにしない。

実写写真、写真調の生成画像、小さな挿絵、実キャプチャを区別する。実録画・実測値・GitHubの証拠を創作しない。未検証事項は脚注だけに隠さず、本筋に関係する場合は図中にも示す。

## TableCastロゴ

呼び出しベル、中央の短・長・短の3本の音声波形、顔にも読める余白を組み合わせる。目の白抜きや左右のほっぺたは加えない。ドーム、押しボタン、軸、台座、波形は同じ線幅と丸い端部で統一する。

- [1:1アイコンSVG](docs/design/logos/tablecast-icon.svg)と[文字付きSVG](docs/design/logos/tablecast-logo.svg)を正本にする。
- アイコンのviewBoxは120×120、線幅9。横組みの文字はInter Boldのアウトラインで、閲覧環境のフォントに依存しない。`preserveAspectRatio="xMidYMid meet"`を明示し、幅と高さを別倍率で伸縮しない。
- 明るい背景は黒、暗い背景は白。透過PNGも同梱する。不要な重複ファイルと旧案は削除した。
- 最小目安はシンボル32px、横組み160px幅。保護余白はシンボル外形の1/4以上。
- faviconは小サイズ専用として16・32・48pxを用意する。[用途別アイコン](docs/design/icons/README.md)はTableCastロゴの形状を維持し、白背景で明暗両方のブラウザー面に対応する。
- 変形、虹色化、外部ロゴとの合体、金属やガラスの効果を加えない。会話の発話者アイコンとして使わない。

旧クロッシュ案、派生比較、画像の試作アーカイブは配布物から削除した。

## 参照元と検証

- [Noto CJK](https://github.com/notofonts/noto-cjk)、[Inter](https://rsms.me/inter/)、[Lucide](https://lucide.dev/)
- [W3C 文字コントラスト](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)、[非テキストのコントラスト](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast.html)
- [Cloudflare](https://www.cloudflare.com/press/press-kit/)、[OpenAI](https://openai.com/brand/)、[TanStack](https://tanstack.com/brand-guide)、[Mastra](https://mastra.ai/blog/rebrand)

現行WebはOS標準を優先するfont stack、一部の明朝体、400・500・600・700、12pxの補足を含む。本資料はそれらの変更を実施していない。現行`rounded-md`や各種shadowの存在も推奨基準と区別する。

雰囲気の参考画像そのものを内蔵画像生成で制作する。HTMLによる組み立てや、実装キャプチャへの置換は今回の試作に含めない。生成されたUI・文字・数値を実装や厳密なフォント検証の証拠にせず、指定値は本Markdownを正本とする。実制作時のUIと、雰囲気を検討する画像を区別する。

検証結果・実キャプチャの範囲・生成情報は[資料一覧](docs/design/README.md)に記録する。
