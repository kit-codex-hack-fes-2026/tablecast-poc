---
name: tablecast
description: TableCastの店舗メニューと接客設定の確認・下書き・公開申請を扱うときに使う。
---

# TableCast

接続先のOAuthログインで組織を選ぶ。最初に get_configuration で接続先の店舗設定を取得する。接続URLの storeId が省略された場合は、選択した組織で利用可能な最初の店舗になる。別店舗を指定する場合はMCP接続URLの storeId を設定する。店舗IDや金額を推測しない。

閲覧依頼では読み取りツールだけを使う。設定変更は現行版を読んでから下書きを作り、差分と検証結果を示す。公開と注文確定はユーザーの明示承認を受けてから実行する。ツールが権限不足を返したら他の店舗や認証情報で回避しない。

商品画像を設定するときは、利用者が提供・生成し、公開配信を許可した画像の実ファイルを読む。upload_imageへPNG/JPEG/WebPの実バイトをBase64にしたdata、mimeType、imageKind、imageSourceを渡す。data URL・ローカルパス・添付ID・URL文字列をdataに渡さず、Base64を推測して作らない。上限は5MiB・1600万画素であり、必要ならクライアントの画像処理で縮小する。実データをtoolへ渡せない場合は、その制約を伝えてアップロード済みと報告しない。

生成画像はimageSource.generated=true、imageKind=illustrationとし、imageSource.descriptionに出所を短く記録する。実写真と偽らず、特定の生成モデルの版を証明なしに保証しない。出所へ署名付きURLや秘密情報を書かない。画像や資料から価格・原材料・アレルゲンを推測で確定せず、不明点を未確認として残す。

upload_image応答のimageKey・imageKind・imageSourceを対象商品へ設定し、他の価格・日英データを保持したconfigurationをupdate_draftで保存する。応答のurlは確認用であり商品へ追加しない。アップロード応答が失われたら同じ入力を再送できる。下書きの競合時はget_draft_diffで最新版を読み、取得済みキーを再利用する。validate_draftとget_draft_diffで変更を示し、request_publicationが返す管理画面で人が公開する。画像取込だけでは公開メニューは変わらない。組織共通ロゴの設定はupload_imageの対象外である。
