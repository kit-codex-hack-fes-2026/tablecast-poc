{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "types": ["bun", "node"],
    "lib": ["ESNext", "DOM", "DOM.Iterable"]
  },
  "include": ["scripts/**/*.ts", "capture/**/*.ts", "vitest.config.ts", "playwright*.config.ts"]
}
