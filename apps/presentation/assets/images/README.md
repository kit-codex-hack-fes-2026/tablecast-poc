# 採用画像

現行台本は `projects/tablecast-main-rerecord.json`。`tablecast-main-*.png` は実アプリの収録から抽出した静止画、`tablecast-logo-*.svg` は技術図でサービスを識別するロゴ。構成図そのものはHTML/CSS/SVGで描画する。

未採用の生成画像・旧版の静止画・制作メモは共有対象外。採用時は台本の参照と `.gitignore` の許可一覧を更新する。
