# TableCast 実装・検証記録

更新: 2026-09-08

ユーザーの指定によりローカル実装を先行する。外部資格を使用する実音声、OAuth、ChatGPTからのMCP接続、公開環境、iPad実機試験は後続の受入とし、未実施を成功扱いにしない。

## 2026-09-08: 音声再開時の会話復元

- エラー後や明示再開で新しいvoice sessionを作った際に、保存済み履歴をモデルへ渡していなかった不具合を修正。同じ店舗・来店の確定字幕と再生済み本文を直近40発話・16,000文字以内で復元し、中断した本文は聞かれた範囲だけを引き継ぐ。
- 過去の依頼・注文承認・ツール実行は再実行しない。接続確認のもしもしで電話応対に変わらず、席での話題を続けるよう指示した。
- API 183件、Python 109件、lint・型検査・format、parity buildが成功。失敗後の別voice session、別の来店の除外、空字幕・ツールイベントの除外、本文量制限を確認した。
- 有料の実音声試験で、T11の新しいvoice sessionへ切り替えた後も、辛口が苦手で香りの良い日本酒が好きという希望を復唱できた。カート・注文は空のままで、検証visitは終了した。プロバイダーのトークン制限自体の解消はこの修正に含まない。

## 2026-09-08: Realtime half-cascadeへの移行

- 先行した音声UI・話者集計・プロンプトの変更を `339561d` にコミットした。以降の移行差分は未コミット、pushなし。
- 通常のRoom dispatchを `gpt-realtime-2.1` の音声入力・テキスト出力とInworld TTSへ変更。公式OpenAI plugin 1.8.0をlockへ追加し、Inworld forkのSHAは維持した。
- 業務ツールはMastraの定義とAPIの操作を共用。ターン認可、call IDの原子的な予約、取消、固定確認、字幕の音声item対応を追加した。字幕は補助であり応答生成を待たせない。話者IDの推測はしない。
- 商品照会をqueryによる詳細取得へ絞り、イベント履歴の重複投入を除去。初期試験で発生したトークン制限と、ツールだけの空応答で無音TTSが待機する問題を修正した。
- `bun run test`: root 12、API 181、Web 11、Python 108件が成功。format・lint・typecheck、parity buildも成功。追加の境界試験は、同一callの並行実行、旧turn、字幕の遅着、自発接客の書込み拒否、固定確認後の新発話承認を含む。
- 有料の実音声試験: 日英の音声入力→Realtime応答→Inworldの有声音声を確認。専用T11で、お造りの照会・カート追加、枝豆1点390円の固定確認とread状態、注文未送信を確認した。検証用のカートを空にし、各検証visitを閉じた。T01のカート21点・13,590円は維持した。
- 実iPad、実店舗の騒音・複数人話者識別、公開環境の受入は未実施。OpenAI字幕に話者IDはない。既存のcascade streamは接続回帰・プロンプト比較に残し、通常音声の自動fallbackには使わない。

## 現在の段階

段階0–4のうち、外部資格なしで動かせるローカル経路を実装・検証した。実装の中間commitは `6c1fb66`。開始時は仕様文書と挨拶用の雛形のみだった。仕様の基準は `7da695b`、不要なClaude/Cursor指示の削除は `aff12ad`、生成した商品画像12点は `94d3352` に保存した。pushは行っていない。

指定されたrepo-local skillは実ファイルが存在しなかったため、同名のユーザー側 `minimum-impl` スキルを読み適用した。

## 基盤の決定

- Bun 1.3.13、Node 24.7以上、TypeScript 6.0.3。Cloudflare公式 `@cloudflare/vitest-plugin` 1.1.4とVitest 4.1.11を使用。
- 通常開発はCloudflare Vite Pluginの補助WorkerでAPIを起動し、Service BindingでWebと接続する。
- 開発資源と秘密情報はworktreeの `.local` に分離する。既存 `.env.local` を自動読込みして有料サービスへ接続しない。
- Web、API、Pythonを分担し、ルートlockfileとmigrationは統合担当が管理する。
- ユーザーの追加指定で、shadcn・Base UI・Tailwind・Lucide・Simple Flagsのデザインシステムを導入。装飾用の見出しや補助文を削除。
- デモは日本酒28種を含む各60商品・3店舗・2組織・36卓。商品画像は生成原本を保存し、R2からImages bindingで640px WebPとして配信する。
- 音声の外部前提がすべて揃った時だけローカルAPIの音声を有効化する。初期カタログのVoice IDは未設定。

## 実行結果

| 確認                          | 結果・証拠                                                                                                                                                                                                                                |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `bun --no-env-file run check` | 全体format/lint/typecheck成功。ルート12件、Python82件、Web音声単体6件、API131件成功                                                                                                                                                       |
| 追加のAPI修正                 | 実workerd・D1・DOでWebSocket終了・再接続、ラストオーダー対象範囲、設定公開の競合・rollback・卓への通知境界を検証                                                                                                                          |
| 更新後のUI部品                | Storybook39件、Web音声単体6件成功。日英・選択・エラー・アクセシビリティ                                                                                                                                                                   |
| ブラウザー                    | 標準音声一覧追加後の本番相当環境で全32件成功。日英注文・提供・会計・OAuth同意、通知切断と復旧、確認期限、設定全体の手動編集・明示公開、設定公開反映、管理画面の通知復旧、文字拡大・キーボード、外部カートの不足補完と競合、閉卓履歴を検証 |
| 実R2・Images                  | 日本酒PNGをHTTP200、640×640 WebP、36,858bytesで取得                                                                                                                                                                                       |
| デモの初期化                  | 新しい居酒屋データを実D1へ投入。3店舗36卓、初期注文15件、event102件、商品180件                                                                                                                                                            |
| history                       | 独立した実D1で600履歴セッション、総注文2,415件、2万超event。再seed件数不変、T01の操作状態保持、外部キー・店舗境界・残額0を検証                                                                                                            |
| 背景進行                      | `demo:play`で正規APIから12操作。T01とT12は進行対象から除外                                                                                                                                                                                |
| 固定patch                     | 公式pluginの隔離環境で13件成功。通常のPython試験とは別扱い                                                                                                                                                                                |
| Git hooks                     | Lefthookのpre-commitとcommit-msgを導入済み。pre-pushは設定せず、自動stageを行わない                                                                                                                                                       |

3worktreeを同時起動し、24予約port、host、Cookie、D1/R2保存先、LiveKit鍵・Roomを分離できた。別環境のCookieは手動転送しても401、別LiveKit鍵も拒否した。片方のreset/stop後も他方の認証・下書き全内容・Roomを保持し、元環境のhealthも200だった。一時worktree・process・container・16port・共有台帳の2予約を片付けた。詳細は `.local/tablecast-isolation-report.json` に残す。

実LiveKitへ2ブラウザーで接続し、440Hz合成音声の41 packets・38,400 samples・RMS 0.070536を確認した。Room削除で両参加者の切断も確認した。実マイクと外部AIは使っていない。`.local/livekit-check.json` に記録し、再実行scriptをCIにも追加した。

## 統合時に修正した問題

ルートの `bun run storybook` で起動しHTTP200を確認した。専用portを使い、自動繰上げを禁止する。一時的な未await Promiseを `no-floating-promises` が検出して終了1となることも確認し、その一時ファイルは削除した。

JSONCの末尾カンマを通常JSONで読んでいた起動不良を、導入済みの標準 `jsonc-parser` に置換した。本番相当実行はCloudflare Vite previewで、公式buildが生成したmanifestから2 Workersへ接続する。ローカル資源名と秘密を含むbuildは共有cacheへ入れない。

Mastraの管理adapter経由のNode初期化を除き、公式 `RequestContext` と `Agent.stream` をHonoから使う。公式 `Mastra({ logger: false })` でprovider例外の会話本文を一般ログへ出さず、HTTPエラーと失敗turnは維持する。Viteが生成した2 Workersを実workerdで起動し、GUI・OAuthを通した。

WebSocketの受信予約コード1005/1006/1015を再送していた不具合を、固定1000の終了応答へ修正した。handlerを削除する案は採用版の実試験で正常終了しなかったため、必要な明示返信を残した。Wranglerの接続台帳も公式 `WRANGLER_REGISTRY_PATH` でworktree内へ分離した。`dev:parity` は実行ごとに最新のbuildを確認して再起動する。

E2Eで卓を開いた後に画面を再読込みする際、選択店舗が初期値へ戻ることを見落としていた。試験内で対象店舗を再選択し、Chromium/WebKit両方の4件が成功した。

通常の全体チェックはサンドボックスによるuv cacheへの書込み拒否があったため、同じ無課金コマンドを必要権限で再実行した。テストをskipや型キャストで通していない。

MCPは認証済み管理者によるクライアント事前登録を使う。匿名DCRの有効化は自動承認レビューで拒否されたため変更していない。この制約下で組織選択・同意・PKCEまでブラウザー試験に合格した。

Wrangler 4.129.0の複数config起動では、リクエスト切断でProxyControllerが `Network connection lost` を致命的エラーとして終了した。予約WebSocketコードの修正後にも再現する別問題であり、[対応する未反映の上流修正](https://github.com/cloudflare/workers-sdk/pull/15207)を確認した。独自patchや自動再起動で隠さず、導入済みの公式Vite previewへ切り替えた。独立したpreview環境では、正常WebSocket終了10回とタブ閉鎖10回の後も毎回health 200、プロセスと認証の維持を確認した。統合した `dev:parity` でもChromium/WebKitの注文・認証4件が20.4秒で成功した。

最終の起動経路でも正常終了10回・タブ閉鎖10回後のhealth 200と認証維持を確認した。previewの秘密ファイルは0600、client成果物への秘密値の一致は0件だった。通常buildへ戻した際は、配備用のWorker名とmanifestへ戻り、ローカル `.dev.vars` が残らないことを確認した。結果を `.local/tablecast-preview-final.json`、T01の日本語画面を `.local/tablecast-kiosk.png` に残した。

## 継続監査での修正

料金プランの対象外商品にもラストオーダー時刻で確認期限を切っていたため、締切後の通常料金の料理を注文できなかった。対象プラン行を含む時だけ期限を制限するよう修正し、対象外の確認・注文成功と対象内の拒否を実D1で確認した。

使用中の卓への開卓要求がUNIQUE制約エラーで500になっていた。空席の時だけセッションを挿入し、その成功時だけ開卓eventを作成する。既存卓と同時開卓の後着要求は409になり、先行セッションとカートを維持する。event保存の途中失敗ではセッションも残らないことを実D1で確認した。

同じ設定公開要求が並行すると、後着のCASが0件でも恒久的な公開キーの条件で履歴を再挿入し、UNIQUE制約エラーになった。SQLite標準の `changes()=1` でこのbatchの更新だけを連鎖し、同じ要求の再送には保存済み結果を返す。確認失効は対象が0件以上なので、1件ずつの更新連鎖の最後に実行する。同キーの別下書き、異なるキーの同じ版、途中失敗の全体rollbackも検証した。

店舗全体の設定公開eventが客の卓event取得から除外され、客画面に古いメニューが残っていた。同店舗の公開通知だけを管理metadataなしで渡し、取得した設定版を客のcatalog query keyへ含める。設定版が変わったら古い商品ダイアログを閉じる。他店舗・他卓・別種の店舗eventを除外する実D1試験も追加した。

設定公開のブラウザー試験はあかり店の空席T10で行う。WebSocket通知を遮断し、正規の下書き作成・価格変更・検証・公開から、HTTP event取得と客画面の価格切替まで通す。終了時は自分の公開版から変わっていないことを確認して元の設定を新規公開し、試験卓を閉じる。公開・復元の版と監査履歴は通常の記録として残る。既存のT12は使用中だったため変更しない。

管理画面の文字を200%へ拡大すると固定幅のサイドバーからラベルがはみ出した。最小幅を維持しながら内容幅で広がるよう修正した。日英・Chromium/WebKitで文字収まりと画面を確認し、キーボードによるログイン・言語切替・ダイアログfocus・画面移動とreduced motionも検証した。実iPadやブラウザー全体zoomの証明とは区別する。

別のHTTPクライアントによるカート変更を、WebSocket切断中の5秒pollと実サーバーへの再接続後の通知から回収した。古い確認を閉じて商品行を重複表示しないことと、ブラウザー時計だけを進めた確認期限の失効をChromium/WebKitで確認した。注文試験もスタッフ日英と単一・複数・数量型の選択へ拡張し、画面の選択内容と実DBのカート・注文snapshotの一致を確認した。

管理ログはAPIが保存する `order.status` の状態、`billing.payment`、`billing.adjustment`、`configuration.published`、`confirmation.prepared` を使うよう修正した。以前は別名のイベントを表示側で仮定し、受付・提供・会計などが汎用更新表示になっていた。実API形式の部品試験を日英で追加し、Storybook22件が成功した。管理画面自身のWebSocketを切断する日英・Chromium/WebKitの4件でも、HTTPからの会計event回収、再接続後の通知、履歴件数と残高の一致を確認した。

MCPの公開確認URLが存在しない `/admin` を指していたため、実在する `/admin/live` へ修正した。公式MCP ClientとStreamableHTTP transport、実OAuth code・PKCE・同意を通す試験3件で、日英商品・speechName・価格・プラン・キャストの更新、検証失敗と修正、差分、承認値だけでは公開できないことを確認した。読取専用scope、OAuth対象組織、店舗team、下書きの店舗境界、要求時点のrole変更も確認した。外部ChatGPTの接続試験とは区別する。

管理画面に商品単価・販売可否・日英表示名/読上げ名/説明の手動編集を追加した。保存は既存PUTとdraft版を使い、未保存中は検証・公開できない。保存結果をquery cacheへ反映し、閉じて再表示しても旧版へ戻らないことを確認した。確認リンクの店舗・下書きはログインを挟んでも保持し、店変更や遅い応答で別の下書きを開かない。日英・両ブラウザーの4件で編集・保存・再確認・検証・明示公開を通し、確認画像8点を目視した。

設定編集をカテゴリ・商品・選択肢・プランの追加/削除、安全情報・画像参照・alias・依存/排他条件・キャスト指示まで拡充した。下書きの作成・破棄もGUIから既存APIへ接続する。未保存の追加を取り消した場合は保存済み対象へ戻し、作成応答待ちに別下書きを編集させない。新しい日英・Chromium/WebKitの4件で保存後の再表示、無効なラストオーダーの検証理由、修正と再検証、破棄、公開catalog不変を確認した。画像28点の表示範囲を分担して目視し、横切れ・固定footerとの重なりがないことを確認した。WebKit画像外の一部下部項目は操作・保存・再表示をE2Eで、表示をChromium画像で確認した。

検証結果はAPI所有の9種類のcode/path/paramsへ変更し、Webで対象名と理由を日英表示する。旧DBの文字列エラーは保存設定から再評価し、読み取りでは版・検証状態・DBを変えない。商品・グループ・選択肢の追加/削除に含まれる価格や安全情報も重要な差分として扱い、構造化内容を省略せず表示する。旧形式互換・公開拒否・全検証コード・追加削除の差分を含めAPI67件、表示部品26件が成功した。

## 自発接客とカート補完

音声configへ自発接客設定を渡し、LiveKitの30秒の相互無言通知から客発話を作らずに応答を開始する。APIが最新の公開設定、卓・音声・言語、空カート、確認状態、スタッフ呼出、進行中turnと180秒の間隔を検証し、D1 batchで開始を予約する。対象外は204とし、参照ツールだけを渡す。設定無効化・新しい客発話・停止後の古い応答と再生通知を拒否する。管理履歴は自発接客の開始を日英で表示する。

実AgentSessionでの応答開始試験により、LLM未登録で `llm_node` のみ上書きした以前の接続不良を検出した。公式 `LLM` / `LLMStream` 拡張に同じ認証HTTPを登録し、通常応答、自発接客、204、客の割込み、停止、503時の再試行禁止の6件を追加して成功した。実STT/TTSは接続せず、APIのHTTP応答だけをfixtureに置換している。セッション録音は明示的に無効化する。

外部経路から必須選択が不足したカートを受け取ると、同じ行の編集画面を一度だけ開く。既に編集中ならその入力を保ち、開いた時点のカート版で保存して古い入力の上書きを拒否する。競合エラーはダイアログ内へ表示する。任意の単一選択には選択なしを設け、追加料金を解除できるようにした。新規ブラウザー2件と全体28件、日英の部品28件が成功した。両ブラウザーの新規画面4点を目視し、入力数量とエラーの表示、固定ボタンの収まりを確認した。

## 閉卓後の来店履歴

店舗ごとの来店履歴と、セッション別に古いログを辿るAPIを追加した。既存のスタッフ・店舗認可と保存済み詳細を再利用し、閉卓日時とIDの組で安定したページ送りを行う。会計は注文スナップショット、確定プラン、調整、支払から既存詳細と同じ計算で表示する。

`0005_tablecast_closed_history.sql` は閉卓履歴の索引だけを追加する。現行ローカルD1へ適用後も旧ビルドのhealth 200を確認した。実行計画では、初頁・次頁とも履歴の索引を使い、注文・支払も既存のセッション索引で検索する。最初のCTE集計案では全注文・支払を走査していたため、ページ内の来店に対する相関集計へ変更した。[D1索引の公式手順](https://developers.cloudflare.com/d1/best-practices/use-indexes/)

履歴のAPI7件では、同時刻と途中の新規閉卓を含むページ送り、205件のイベント、会計と確定プランの保持、店舗・端末・所属取消の境界、不正queryの400、閉卓後の呼出解除拒否を確認した。全体の静的解析とAPI85件が成功。日英の部品32件は空のStorybook cacheからも成功した。

履歴の新規ブラウザー4件で、実APIのページサイズだけを2へ変更し、一覧カーソルとログの全件一致・重複なし、注文snapshot原文、閉卓の変更操作非表示、言語・店舗切替を確認した。100件超の境界はAPI層の205件試験で検証し、ブラウザーで大量の架空イベントを追加しない。

通知が入った際にも専用ログqueryを即時更新するよう接続し、最新版の全ブラウザー32件が成功した。日英・両ブラウザーの一覧・注文・ログの画像12枚を確認し、日時列と金額、表示範囲の文字やタブに重なりがないことを確認した。既存試験の設定復元・下書き破棄・清算閉卓も成功し、こもれびT12とあかりT10の空席、health 200を確認した。

## 音声の診断IDと設定候補の復帰

API発行の `X-Request-Id` を `traceId` として認可済み卓・音声session・turn・Mastraの公開 `runId` へ対応付けた。受付・生成終了・拒否・中断・失敗・204を区別し、本文読取り待機中のHTTP取消も正常終了として記録しない。Mastraの汎用loggerは無効のまま、本文・話者・認証・provider例外を診断から除外する。実workerd/D1/Mastraの新規7件と既存23件が成功した。

Pythonは各HTTP応答のheaderを本文より先に読み、同じturnへLiveKitの生成IDと公開再生IDを対応付ける。通常発話と固定確認の再生IDは別々に持ち、旧turnの後処理にも元のIDを残す。新規10件を含むpytest82件が成功。実AgentSessionで通常応答・確認再生・次turn開始後の旧終了503を通し、成功metricsのID一致と秘密の非漏洩を確認した。失敗時にSDKがmetricsを発行しないことも確認し、架空のmetricsを補わず診断と公開errorで追跡する。

ローカル実行版を固定値 `local` からGit HEADへ変更し、未commitの変更には `-dirty` を付ける。独立Git fixtureで追跡変更・未追跡・stage済み・無視対象を確認した。最新の公式Vite previewで未認可HTTPを送り、応答headerと実JSONログのID・実行版が一致し、未認可のturn情報がないことを確認した。結果は `.local/tablecast-diagnostics-http.json`。WebとAPIのhealthは200だった。

キャスト音声を未設定へ保存した後でも、同店舗の公開済みvoiceへ戻せるようにした。公開設定・保存下書き・編集中の値だけを候補へ使い、店舗別queryと公開通知の更新を接続する。復帰・重複排除・別店舗への候補切替の部品1件を含むStorybook33件が成功。最新版の設定編集E2E4件も日英・Chromium/WebKitで成功し、英語Chromiumの音声欄と日本語WebKitの保存領域の画像を目視した。新しいprovider一覧を実装した結果ではない。全体のformat/lint/typecheckとルート8件・Web単体6件・API92件・Python82件も成功した。

## 標準音声の一覧と公開検証

現行Inworld Voice APIを使う標準音声の一覧を、認可済み管理HTTPと読み取りMCPで共用した。主言語のja/enとSYSTEMだけを対象とし、名前・ID・主言語と次ページのカーソルだけを返す。固定origin、Read権限の別キー、5秒timeout、256KiBの本文上限を使い、provider本文や例外は外へ返さない。

新しい非null IDへ変更する時は、下書き検証と公開直前に単体metadataを取得する。未知ID・Custom Voice・主言語の不一致は対象欄の検証理由へ変換する。公開済みの値を維持する編集と未設定への解除は外部未設定・障害時も継続し、成功した公開の同じ再要求は再照会しない。新しいAPI19件とMCP2件を含むAPI113件が成功した。provider応答は境界のfixtureであり、実Inworldへの接続はまだ行っていない。

キャスト欄を開いた時だけ、店舗・言語別に候補を取得する。選択では表示名を見せてIDを保存し、追加取得・失敗後の再試行・候補の重複除去と保存済み値の保持を確認した。店舗変更時は旧取得を中止し、遅い別店舗の候補を混ぜない。Storybook35件と本番相当環境のブラウザー全32件が成功した。再試行ボタンの縦位置を整えた後も設定編集4件を再実行し、日英の画像を確認した。

一覧用キーの設定は公式Wranglerで型生成した。隔離したruntime fixtureで、キーがAPIの秘密ファイルだけに入り、合成用キーや公開configへ流れず、ファイル権限0600と音声受付falseを保つことを確認した。実際の開発用キーを保存・利用した試験ではない。

## Python Agentの配備用イメージ

公式uv 0.11.26/Python 3.13の実manifest digestと既存uv.lockに固定したDockerfileを追加した。build contextをmanifest・README・srcに絞り、非editableなインストール済み環境だけを最終imageへコピーする。linux/amd64でbuild、ネットワーク無しのCLI・Agent import・Silero VAD読込み、非root UID 10001が成功した。

外部通信不可の専用Dockerネットワークで、別のローカルLiveKitへAgentを待受登録し、health 200を確認した。外部AI資格・Room job・ホストへのport公開は使わず、専用コンテナ・ネットワーク・鍵ファイルは削除済み。image IDは `sha256:6461444a346c34ca508de30da47dbbfacaa92693d652d77c0ea53a3008b2bc04`、結果は `.local/tablecast-voice-container-report.json`。既存の開発LiveKitはこの試験では操作していない。

## 公開環境の初期セットアップ

`db:bootstrap` を追加し、staging/productionの明示設定と所有者だけが読めるJSONから初期管理者・組織・標準team・店舗・初期公開版・卓を作る。D1だけの一時Wrangler設定を使い、既定は対象表示と入力検証に留める。適用には `--apply` とlocal/remoteの明示が必要で、開発用dotenvや共有ランタイムを使わない。管理用HTTP経路は追加していない。

専用D1に実CLIでmigrationを適用し、事前確認でDBを作らないこと、実際の初回作成、再実行の拒否と件数不変を確認した。ルートの4件で秘密入力の権限・symlink拒否とログ非漏洩も確認。APIの新規16件と既存認証7件ではログインと店舗境界、同時競合、入力検証、組織作成の中途失敗、店舗・公開版・卓の一括rollbackを確認した。認証API間の原子性はないため、途中の認証資源は自動削除・再採用せず判明したIDだけを報告する。実アカウント・遠隔D1は未使用。

## 客画面の拡大文字と長い商品説明

200%文字の客画面で、ヘッダーの操作が店舗名へ重なり、かごの見出しが固定高からはみ出す問題を画像で確認した。ヘッダーは内容幅に応じて折り返し、言語切替は文字幅より縮めず、かごボタンは内容に合わせて高さを確保する。1024×768の通常表示と、日英の拡大文字・768×1024縦画面を検証し、修正後の画像も確認した。

商品名150文字・説明3000文字の合法な入力でも、説明を固定領域に置いていたため追加ボタンが画面外へ押し出された。商品名・説明・価格を既存のスクロール領域へ移し、閉じる・追加操作を外側に保つ。修正前はviewport高900pxに対しボタン下端1130.1875pxで失敗し、修正後はTabによる選択欄へのスクロール、選択、追加送信まで成功した。

日英の依存・排他条件は、選択済みの排他対象を外し、別グループの必要な選択肢を追加して正しいカート行を送る2ケースを追加した。業務の判定は既存APIに維持し、部品側へ価格・選択条件の判定を複製しない。長文1件と合わせてStorybook38件が成功した。

ヘッダー・かごの修正後にE2E全32件が成功した。その後の縦画面の言語切替幅と長文ダイアログの修正については、影響する注文・文字拡大・不足選択・設定公開の12件をChromium/WebKitで再実行して成功した。最終コードで全体のformat/lint/typecheck、ルート12件・API129件・Python82件・Web音声単体6件も成功した。文字拡大はCSSによる200%表示であり、実iPadやOS・ブラウザー全体のzoomの確認とは扱わない。

## 長時間営業時の卓状態

卓の表示ログは最新100件に限るが、管理画面がその先頭時刻を開卓時刻として扱い、同じ範囲の会計依頼eventで会計待ちを判定していた。100件を超える営業では経過時間が短く表示され、未精算の会計依頼が集計から消える。

APIの卓状態に既存DBの開卓時刻と、対象店舗・来店セッションの全eventに基づく会計依頼の有無を含める。管理画面はこの状態を使い、表示ログの件数に依存しない。会計待ちは依頼済みかつ未払残高がある卓とし、ログ上限・DB構造・決済操作は変えない。

実D1の回帰2件は修正前に失敗し、修正後に成功した。全体のformat/lint/typecheck、ルート12件・API131件・Python82件・Web音声単体6件、Storybook39件が成功した。本番相当のWeb/APIを再ビルドし、Chromium/WebKitの全32件も成功した。日英の注文フローで客の会計依頼により管理画面の会計待ちが1件増え、支払い後に元の件数へ戻ることを確認した。

## TailwindとUI責務の整理（2026-09-06）

`styles.css`を2,452行から115行へ縮小し、読み込み・テーマ・全体既定値に限定した。画面の固定ピクセルや色を任意値へ転記する方式を取りやめ、Tailwind標準の余白・文字サイズ・角丸・ブレークポイントと共通色へ統合した。元の各ピクセル値の完全一致ではなく、標準スケールに揃えた表示となる。

Dialog、Table、Checkbox、RadioGroupItem、NativeSelectを業務を持たないUI部品へ集約した。商品カスタマイズ、注文かご、管理ナビゲーション、卓集計、開卓、端末承認をfeature単位へ分離し、注文の承認・認可・API操作は元の所有側に維持した。AGENTS.mdとUI部品READMEへ方針を記載した。

Webのformat、lint、typecheck、音声単体6件、Storybook41件、本番buildが成功した。幅390・834・1280・1440pxで商品選択、会話、卓一覧などを撮影・比較し、390pxの英語管理ナビゲーションに横はみ出しがないことも確認した。buildには依存先gray-matterの既存eval警告が残る。

この変更後の実DBを通す全画面E2E、実iPad、再配備は未実施。上記以前のE2E成功記録を、この変更後の検証結果としては扱わない。

## 会話UIとパッチ版の実音声接続（2026-09-07）

会話履歴と聞き取り・思考中の行を同じ部品へ統合した。AI ElementsのConversationでスクロールを管理し、Streamdownで逐次本文を表示する。実音声の波形、話者IDの色分け、ツールの実行中・完了・エラー・中断を表示する。デバッグ切替では共通・店舗の接客プロンプトと、その接続で受信した生成原文・演技タグを確認できる。

会話内の商品カードは既存の商品選択UIを共用し、注文確認カードは現行スナップショットのGUI確認を開く。Agentの `showProducts` はAPIカタログの商品IDだけを受け付ける。GUIとAgentのタブ操作は認可済みの同じ業務操作を使い、migration 0006で `uiSection` を卓に保存する。カート追加は必要な選択が揃ったら実行し、注文確定の明示承認と混同して何度も許可を求めない接客指示にした。

Mastra・AI SDK・`gpt-5.6-luna` を維持し、通常返答には状況に応じた英語の演技タグを毎回生成させる。Pythonは本文とタグをInworld TTSへ渡し、画面・永続履歴ではタグを除く。挨拶・お礼だけの返答では業務照会を要求しない。TTSの公開バッファ待ち設定は300msとした。

Inworld公式pluginの話者パッチを公開fork `ReoHakase/tablecast-livekit-agents` の `2f36303643f00c9c521d9dc27a785a6f273bbfb1` へ固定して適用した。coreとSileroは公式1.8.0を維持する。日英の実Inworld試験で、演技タグ付き合成・確定認識・話者0・単語情報を確認した。短いお礼では単語時刻が届いても話者が欠損することを実測し、前の話者を補完せず未識別と表示する。

ローカルLiveKitの専用T11で実STT→Mastra→TTSを試し、日本酒2種の画像カード、タブ切替、話者ラベル、逐次本文、再生済みログを確認した。最終パッチ版の1回で、確定認識から字幕開始5.39秒・音声開始6.49秒。別のお礼1回は業務ツールなしで字幕0.80秒・音声1.89秒だった。合成した客音声による限定計測であり、平均値や人手の自然さ評価ではない。試験後は音声を停止し、試験用来店を閉じた。利用中のT01のカートは維持した。

API全161件、Python90件、Web単体10件、関連Storybook22件が成功した。その後の接客プロンプト変更でも関連API48件・会話Storybook12件を再実行し、型検査・lint・formatを確認した。実ブラウザーではデバッグ切替とプロンプト本文、GUIタブ変更の保存・再読込みを確認した。

配備用linux/amd64 imageも同じforkでビルドし、ネットワーク無しで非root UID 10001・インストール済み完全SHA・話者option・Silero読込みを確認した。image IDは `sha256:ffdd1fd4827079d9f2fc6310a5ca14e728ead4941ba5971ca65d5ef6f8194b53`。上流PRの送信、実iPadのマイク・AEC、実店舗の複数話者・騒音・重複発話、感情表現の人手評価は残る。

カート追加の実音声試験では、辛さの指定が中皿と誤認識され、任意条件を落とした追加になるケースを確認した。指定済みの任意条件も全て反映し、登録選択肢に対応しない条件は無視して追加せず、その条件だけ聞き直すようツール指示を補った。STT精度やLLMの条件解釈を完全に保証するものではなく、最後の版付きスナップショットと明示承認を維持する。
修正後は合成音声の辛さをちゅうからと明瞭に読み、唐揚げ一皿・ポン酢・中辛が一度の依頼でカートへ追加され、注文0件を保つことを確認した。実際の返答にも状況に応じた演技タグが付いた。試験用カートを空にしてから来店を閉じた。誤認識された語の聞き直し自体を実音声で再評価した結果ではない。

## 未実施の受入

初期bootstrapは対象公開環境での実行が残る。標準voice一覧・選択・実在性と主言語の検証は実装したが、実在providerでの確認と日英の試聴は外部設定後に行う。実店舗での自発接客の無言検出・頻度・自然さも未評価である。

GitHub上のCIは未実行。ローカルで同じ静的解析・各層試験・本番相当E2Eを実施した。

実Inworld/LLMとローカルLiveKitの往復は上記の範囲で確認済み。実iPadのAEC・騒音・割込み、Googleログイン、ChatGPTからの実MCP接続、公開配備と復旧は未実施。話者パッチは公開forkの依存として適用済みだが、実店舗での話者精度は未評価である。PoC全体の外部受入完了とは判定しない。

[構成の実現可能性・課題・改善点](feasibility.md)と[公開・移行・復旧手順](deployment.md)に詳細を記録する。

## 会話配置・個別商品ページ・話速の改善（2026-09-07）

会話とメニューをタブレットで並べ、波形・音声停止／再開・話速スライダーを会話の下へ集約した。空の中断吹き出しを除き、ツール結果を対象時刻へ配置した。自動追従は履歴を上へ移動すると解除でき、最新ボタンで再開できる。文字と境界のコントラストを上げた。

商品・オプション・最終確認を画面内のページへ変更した。カートにも商品画像と版検査付きの数量増減を表示する。migration 0007でselectedProductId/speechSpeedを追加し、AgentとGUIで商品詳細の選択を共有する。古いHTTP応答で画面を巻き戻す競合と、最新状態より先のイベントcursorを返して変更を取り逃すAPIの読取順を修正した。

話速は既定1.15、0.8〜1.5で調整でき、次の発話から反映する。Pythonは2秒周期で同期し、停止・認可失効・別音声セッションの遅延応答を適用しない。通常返答は同じwarm/composed/conversationalの演技を維持し、新しいTTS contextへ重複してresetを注入しない。短い肯定や訂正を前の文脈へ結び付け、同じ質問・導入を繰り返さない指示を追加した。

不足していた23商品の画像を組込みimagegenで生成し、assets/demo/generated-menu-2026-09-07.jsonへプロンプトと対応表を保存した。全23点を一覧で目視確認し、R2へ取り込んだ。既存の合成3店舗・180商品へ画像と商品別の原材料注記だけを版付き公開操作で反映した。含有アレルゲンの既存値と未確認状態は維持し、商品詳細に含有項目を表示する。利用中T01の注文内容・数量は変更していない。

専用T11の実Inworld STT→Mastra gpt-5.6-luna→Inworld TTSで、さばの商品詳細→次の発話で注文かごへの遷移を確認した。二往復は同一STT streamId・speakerId、両返答は同じ演技タグだった。試験卓は終了後に閉じた。合成入力による限定試験であり、実店舗騒音や複数人の短い発話の識別精度、人が感じる自然さは未評価。

API167件、Python96件、Web単体11件、seedの実D1試験を含む5件が成功した。Pythonの6つの追加試験は実プラグインとローカルWebSocketを使い、二往復の接続維持と速度変更の境界を検証する。

Storybook48件とChromium／WebKitの日英E2E計6契約が成功した。最後のフッター調整後にもWebKit日本語E2Eを再実行し、200%の縦画面で金額と確認ボタンが重ならないことを確認した。専用T12は試験後に空席へ戻し、利用中T01のカート3品・3,080円を維持した。

## 話速・言語ツール・話者識別の必須化（2026-09-08）

話速の既定を1倍、範囲を0.5〜2倍、刻みを0.1に統一し、スライダーの両端へ亀とうさぎを表示した。migration 0008は既存卓を保持し、旧既定1.15を1へ、既存の個別速度を0.1刻みへ移す。

MastraへsetSpeechSpeedとsetLanguageを追加した。GUIと同じ認可・業務操作を使い、古い音声turnからの言語変更を拒否する。言語変更は音声資格と確認を失効させ、ツール完了を同時に記録し、新言語の音声は明示再開を待つ。

話者IDとstreamIdがない客発話はLiveKitの公開フックで履歴・生成前に止め、画面へ話し直しの案内を送る。API側も未識別を拒否する。自発接客には客の話者を要求しない。Inworldの識別成功率を保証する変更ではなく、未識別を推測や前の話者で補完しない。

API全172件に加え、停止後の言語変更拒否を追加した関連49件、Python98件、Web単体12件、Storybook48件、ルート12件が成功した。型検査・lint・format・本番相当buildを確認した。

専用T11の実Inworld→Mastra→Inworldで、話速1.6倍と英語への変更・音声停止を確認し、二発話のSTT接続・話者IDが一致した。初回は返答再生中に次の合成入力を送って文頭が欠け、言語変更にならなかった。再生完了後に入力する試験で成功した。これは割込み時の認識精度を解決した証拠ではない。試験卓は終了し、利用中の卓の注文内容を変更していない。

## 話者の多数決と欠損時の継続（2026-09-08）

利用者の指示により、前節の話者未識別による停止を撤回した。PythonのStopResponse、APIのVOICE_SPEAKER_REQUIRED、専用エラー表示を除き、識別失敗でも通常の会話を続ける。Inworld STT/TTSとSHA固定したpluginは維持する。

識別済み単語だけの多数決をTableCast側で行い、最多が一人なら発話の代表話者とする。同数・全語未識別なら未設定とし、単語の元ラベルや前発話の話者を変更・補完しない。画面とAPIへ同じ代表話者を渡す。

Python101件、Web単体11件、関連API42件、型検査・lintを確認した。実pluginとローカルWebSocket・LiveKit Sessionの結合試験では、全語識別・多数決・全語未識別の各条件で二往復が継続し、画面とAPIの話者が一致することを確認した。外部Inworldの認識精度を再計測したものではない。

## 話速上限・白黒の音声UI・ツール前の声かけ（2026-09-08）

利用者から1.6倍以上で無音になる報告を受け、運用上限を1.5倍へ変更した。既定1倍・0.1刻み・下限0.5倍は維持し、GUI・API・Python設定・DB制約を一致させた。migration 0009で既存の1.5倍超を補正し、卓やカートを保持する。Inworldの全環境に共通する公式上限を意味しない。

共通色を白黒へ変更し、ボタンへ半透明の白・内側の光・控えめな外側の影を加えた。音声停止／再開は黒地・白文字・虹色リングに統一した。下部の虹色orbは実LiveKit音声の音量へ反応し、波形と並べる。停止時と動きを減らす設定では装飾アニメーションを止める。基本トークン同士の文字コントラストは本文16.27:1、補助文字6.38:1で、ブラウザーでも実表示を確認した。

照会や操作を要する依頼には、最初のツール前に短い声かけを一度だけ生成する指示を追加した。Mastraの既存textStreamをそのまま使い、追加のモデル呼出しは行わない。関連API50件には、最初の本文をHTTPから読める時点でツールが未実行であることと、ツール後の続きが同じ応答に流れる試験を含む。Python101件、Storybook48件、型検査・lint・format・本番相当buildが成功した。

専用T11の実Inworld STT→Mastra gpt-5.6-luna→Inworld TTSで、先行する声かけと商品ページ表示後の返答、再生完了を確認した。この一往復は確定認識から最初の本文まで0.94秒、可聴音量の音声まで4.38秒だった。実行時間の保証ではなく、TTSの待ち時間は残る。初回は演技タグの先頭一致検査に失敗し、診断再試行では同じタグをツール前後で重複生成した。声かけと演技書式の遵守はモデル依存で、常に同じ文・タグ配置になる保証はない。試験卓は各回終了し、利用中T01の注文内容は変更していない。

## 音声を主とする注文案内と状態別のorb（2026-09-08）

カードを見られるため情報を省略する指示を撤回し、画面を見ずに判断できる音声案内へ変更した。選択を求める際は候補・追加料金・必要数を読み上げ、任意オプションは希望がなければ確認を挟まない。明確な単品注文は一つとしてカートへ追加し、温度・容量など不足する必須項目は一度に案内する。カートツールに残っていた一つずつ質問する指示も変更した。曖昧な数量や安全情報、注文送信前の固定読み上げと明示承認は維持する。

ElevenLabs Orb、LiveKit Agents UIのWave/Aura、Paper Shadersを比較し、Paper Shaders 0.0.80のSmokeRingとMotion 13.2.0を採用した。shaderを自作・転載せず公開コンポーネントを使用する。波の流速・密度・半径・厚みで状態を分け、Motionで変化を補間する。音量だけでなく周波数分布も形へ反映し、無音の思考・処理も区別する。停止・エラーは静止し、記号と状態名も併記する。実音声波形は20fpsに抑え、非表示・動きを減らす設定では描画を止める。

参考: [Paper SmokeRing](https://shaders.paper.design/smoke-ring)、[Motion animate](https://motion.dev/docs/animate)、[LiveKit Audio visualizers](https://docs.livekit.io/frontends/agents-ui/audio-visualizer/prebuilt/)、[ElevenLabs Orb](https://ui.elevenlabs.io/docs/components/orb)。

Storybook51件、関連API50件、型検査・lint・format・本番相当buildが成功した。実ブラウザーで8状態の見た目を比較し、ChromiumとWebKitの連続スクリーンショットで、停止中は変化なし・無音の思考中は変化あり・動きを減らす設定では変化なしを確認した。発話中のツール実行併記と、停止後の古いツールの中断表示も検証した。

実Inworld経由でお造り一つを一往復で追加し、任意の薬味選択を要求せず、未承認の注文送信がないことを確認した。認識が崩れた薬味の質問には余分な聞き返しが残ったため、全実音声ケースの成功とは扱わない。STTを通さない実Mastraモデル・業務API試験では、単品名だけの注文を数量一つで追加し、薬味の候補と無料／レモン三十円を案内できた。日本酒で容量・温度や追加料金を省略する結果も観測し、競合するツール指示と基準容量の解釈を修正した。最終試験では、月凪の温度四候補と、容量四候補それぞれの価格（六十ミリ620円、九十ミリ780円、百八十ミリ1,140円、三百ミリ1,600円）を同じ発話で案内した。未選択項目を勝手に確定せず、注文送信もなかった。タグの重複や途中の説明を完全に防ぐ変更ではない。

有料試験は専用T11のみ使用した。初回の試験スクリプトがcartオブジェクトを配列と誤って検査し、カートが残るため閉卓が拒否された。試験のvisit IDを指定して専用カートだけを削除・閉卓し、以後は後処理でカートを空にしてから閉じるよう修正した。利用中T01のカートや注文は操作していない。

## 管理コンソール・デモアカウントの見直し（2026-09-09）

参照スターターのサイドバーとinset構成を採用し、組織切替・ページ遷移・ユーザーメニューを集約した。本文・操作16px、補助14px、濃い補助文字、白い入力欄、Googleロゴ、ユーザー画像・名前・メールを揃えた。組織作成は必要時に開き、未所属時には作成・招待メールからの参加を案内する。削除・解除は対象を確認する。固定ヘッダーとスクロールバー領域、読込中の共通枠を維持する。

emulateの再起動で変わるsubを開発用issuerと確認済みメールへ正規化し、seedは旧モック連携だけを統合する。2組織・3店舗に管理者と担当スタッフを配置し、組織とチームに店舗名を表示する。起動スクリプトから旧ハッシュ・旧単一ラベルドメインの互換分岐を除いた。

API188件、Web単体11件、root14件（seed回帰の再実行を含む）、型検査、lint、formatを確認した。Chromium/WebKitの認証・名前変更・組織作成・招待・未所属導線・同一メール連携が5件成功し、仮想パスキーはChromiumで成功した。既存のWebKit仮想認証器1件はCDP非対応により対象外。E2Eが生成した組織・仮想パスキー・プロフィール変更は終了時に片付ける。

実ブラウザーでアカウント、組織、設定画面を目視し、狭幅の折返しと、835px時の横スクロールなし・表示文字14/16pxを確認した。既存ローカルDBをバックアップし、過去の受入試験由来の組織と仮想認証器を限定整理した。ホストT01のcart_json/cart_versionは変更前後で一致した。実Google・実機パスキー・実音声の再試験は今回の対象外。

## 店舗単位の管理・専用ページ・型付きAPIへの整理（2026-09-09）

店舗とBetter Authの組織を一対一に揃え、チームを使う店舗内の階層を廃止した。migration 0011は管理者と担当スタッフの権限を店舗ごとに移し、分割前の複数店舗に対するOAuth同意を失効する。店舗作成APIは店舗・組織・責任者・卓をまとめて保存し、店舗を伴わない組織だけの作成を拒否する。固定データとFakerを組み合わせ、3店舗・36卓・180商品と店舗別スタッフを用意する。既存のプロフィール・商品編集・カート・注文はseedで上書きしない。

商品詳細・編集・変更確認、来店履歴の詳細、端末登録、招待、店舗作成を専用URLへ分けた。表示中のメニューを一覧と詳細から確認でき、編集内容の保存・検証・公開を段階別に表示する。詳細をSheet/Dialogへ隠さず、破棄・解除の確認に限ってAlertDialogを使う。Base UIの選択部品とTanStack Table/Form/Queryを使い、ユーザー画像・氏名・メール、役割の色とアイコン、作成・更新・有効期限を各操作の文脈へ配置した。

APIの公開型をHono RPCへ接続し、Web側のレスポンススキーマの二重定義を削除した。業務操作と状態をfeatures、共通表示をcomponents、業務を知らない部品をcomponents/uiへ整理した。React Doctor・a11y・TanStack Query/Router・Tailwindのlintを導入し、警告を修正した。通知切断中の再取得と再接続でイベントが欠落しないよう、独立したスナップショット取得によってイベントカーソルを先送りしない。

Better Auth公式のOAuth 2.1 Providerは導入済みのパッケージ1.7.2を継続する。公式のopaqueトークン設定で失効を即時反映し、MCP連携ページから対象の同意とアクセストークン・更新トークンをまとめて解除する。承認URLの署名付き検索値はスターターの方式で保持する。旧JWT方式の接続は再認可が必要になる。

型検査・lint・format、API192件、Web単体13件、seed等14件、Storybook51件が成功した。実ブラウザーで店舗選択・メンバー・商品詳細・MCP連携を目視した。受入試験は専用D1/R2・emulate・Mailpitとビルド済みWorkersへ隔離し、終了時に破棄する。

Chromium/WebKitの日英E2Eは39件成功した。既存のWebKit仮想パスキー1件はCDP非対応のため対象外で、Chromiumでは登録・再ログインが成功した。MCPの承認・利用・日時とscopeの表示・画面からの解除・同じトークンの401応答、200%表示とキーボード移動、通知切断時の回復、商品編集と明示公開、注文・提供・会計を含む。初回はWebKitの商品詳細遷移と再読み込みが競合したため、詳細URLと見出しの表示を待ってから再読み込みする試験へ直し、全件を再実行した。

開発DB移行前後で、ホストの既存219来店のカート・版と76注文のスナップショット、コンテナの既存33来店と15注文を比較し、保持を確認した。双方とも店舗と組織は3対3、外部キー違反は0件だった。ホストは今回取得したバックアップ、コンテナは既存バックアップとの比較である。実Google、実機パスキー、実音声、公開環境の受入は今回実施していない。

## 端末登録のQRリーダー（2026-09-09）

端末登録ページへカメラと画像のQR読取りを追加した。`qr-scanner@1.4.2` を必要時に読み込み、ブラウザー内で解析する。同じ環境の端末登録URLからコードだけを取り出し、選択済みの卓と明示承認の操作を保持する。カメラの拒否・画像の読取失敗には代替操作を案内し、成功・停止・ページ移動・映像開始失敗でカメラを解放する。

Web単体21件、型検査・lint・format、ビルド済みWorkers上のChromium/WebKit各3契約を2回ずつ、計12回検証した。実際の端末QRの画像読取り、模擬映像からの読取りと停止、エラー後の手入力、URL復元、明示承認後の登録を確認した。WebKitの試験ではnative mediaDevicesへのメソッド差替えが保持されなかったため、ページ表示後に媒体オブジェクトを試験用入力へ置換した。製品コードへ試験用の分岐は追加していない。開発コンテナへ依存を反映し、端末登録ページを目視した。物理カメラ・実iPadでの読取は未実施。

## サイドバーと注文ペインの幅調整（2026-09-09）

`react-resizable-panels@4.12.4`を共通のResizable部品へ接続した。管理サイドバーは幅調整と折りたたみ、注文画面は会話・注文の比率調整に対応し、矢印キーでも操作できる。比率をブラウザーに保存して再読込で復元する。狭い画面では管理ナビゲーションの開閉を維持し、注文画面は縦の比率を調整する。画面内の独立スクロールを保ち、Googleアイコンの既定サイズも修正した。

Web単体21件、型検査・lint・formatと、隔離環境のChromium/WebKit計12件のE2Eが成功した。ドラッグ・矢印キー、幅保存、折りたたみ、画面幅変更、注文タブ保持、文字200%表示、日英の注文・提供・会計を検証した。開発画面と受入試験の画像を目視し、ライブラリのインライン高さ指定で管理領域が画面を超える問題を修正した。実音声接続中のリサイズと実iPadのタッチ操作は未実施。

## メニューの階層ナビゲーション・外部連携の案内・店舗画像（2026-09-09）

メニュー・接客をBase UIのCollapsibleで展開し、商品・カテゴリ・プラン・キャスト・変更一覧へのリンクをサイドバーへ移した。直接URLと再読込でも現在のページを展開し、編集中は対象の変更IDを保持する。ページ内の重複タブは除いた。

MCP・Skillsをプラグイン導入・手動導入・OAuth接続の個別ページへ分けた。公式資料に基づくChatGPT/Codexのmarketplace・開発者モード・ローカルリポジトリ手順と、現在の環境に合うMCP URL、OAuth方式、scope、実際のSKILL.mdの配布を用意した。GitHub上の現行パッケージはlocalhost向けであり、公開ディレクトリへの掲載済みとは表示しない。接続一覧は共通のTanStack Tableを使い、有効状態、店舗、scope、承認・更新日時、アクセス／リフレッシュトークン期限と解除操作を表示する。

店舗設定ページで角丸の正方形アイコンを保存できる。店舗・組織の一対一対応を維持し、Better Authの組織logoと既存R2配信を使用する。責任者・管理者だけが更新でき、ユーザー画像と形式・サイズ検証を共有する。seedは架空ユーザーと店舗の画像を生成し、未設定分のみ補完する。開発コンテナへseedを反映し、既存の店舗画像とプロフィールを上書きしないことも隔離環境で検証した。

API195件、Web単体21件、seed等14件、型検査・lint・formatが成功した。Chromium/WebKitの20件でメニューの展開、日英の導入ガイドとスキル保存、画像の読込・変更・再表示、OAuthの明示同意・scope・日時・取消後401、200%表示、商品公開、ペイン幅の保存を確認した。開発画面でカテゴリ・店舗画像・導入ガイドを目視した。受入中に旧管理URLのSSRで幅保存フックがlocalStorageへ触れる問題を見つけ、サーバー描画時の保存処理を分離した。ChatGPT/Codexへの新規インストールと公開環境への接続試験は今回実施していない。

## 本番Google OAuthとPR別CI/CD（2026-09-09）

mainを`tablecast.kit-codex.workers.dev`、同一repoのPRを独立したWorkers/D1/R2/DO/Containersへ配備するActionsと、close時の所有資源cleanupを追加した。本番は実Googleの資格をEnvironment secretから渡し、PRはAccessと専用emulateを使う。外部APIキーは既存値をRepository secretへ登録し、内部鍵は環境別に導出する。PR専用の初回seedは対象と所有情報を検査し、再配備で営業中データを上書きしない。

型検査、配備境界と実D1 seedの11件、本番とPR設定のWorkers build・Wrangler dry-run、linux/amd64の両Dockerイメージを確認した。ローカルWranglerで実LiveKitに接続した音声Containerを起動し、重複予約とdrainの拒否で同じContainerが保持されること、予約中のidle期限で停止しないこと、解放後のSIGTERM停止を確認した。通常の受付拒否をDO入力ゲート内でthrowするとDOがリセットされる問題を検出し、ゲート外で拒否を返すよう修正した。OAuthイメージの起動と架空ユーザーの選択画面も確認した。

Cloudflare側のR2・Access有効化、Actions用API tokenとAccess資格・policyが未設定で、遠隔配備・cleanupと本番Googleログインは未検証。実音声job中の継続性、cold startの本番値、強制終了からの復旧、同時sessionの最適数、月300分で30 USD未満の費用受入はIssue #34に残る。ローカルの起動・予約試験はこれらを代替しない。

## iPad向けPWAと自動キャッシュ更新（2026-09-12、Issue #98）

Serwist 9.5.12で卓上・店側の個別manifest、アイコン、日英の追加案内とオフライン復帰画面を追加した。TanStack StartとCloudflareの全環境ビルド完了後に公式Vite APIでクライアント資産を注入する。メニュー画像は投入前にSHA-256をキーへ含め、アップロード完了後に既存のimageKeyとして公開する。固定キーの互換性は維持し、長期保存はハッシュ付き画像だけに限定する。

更新は起動・前景復帰・通信復帰・前景中60秒間隔で検出し、同じSWを共有する全画面が安全と応答するまで待機する。接客・端末登録・未保存フォーム・QR読取・送信中を避け、適用直前だけ入力を止めて一度再読込する。キャッシュは画像256件・最終利用から30日で整理する。

全体チェック、Webブラウザー統合68件、PWAと公開のChromium/WebKit6件、日英の注文フロー4件が成功した。容量不足はChromiumの保存境界で一度だけ発生させて復帰を確認した。途中のE2Eでは再読込の待機開始が遅い試験を修正し、コンテナ削除の一時失敗は対象を再実行して成功した。実iPadと遠隔配備の未実施範囲は[受入条件](acceptance.md#ipad向けpwaの追加受入2026-09-12issue-98)に残す。
