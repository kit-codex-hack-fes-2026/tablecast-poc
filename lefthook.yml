lefthook: bunx --no-install lefthook

pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{ts,tsx,js,json,jsonc,md,yml,yaml,css}"
      run: bunx --no-install oxfmt --check {staged_files}
    lint:
      glob: "*.{ts,tsx,js}"
      run: bun run codegen && bunx --no-install oxlint --type-aware --deny-warnings {staged_files}
commit-msg:
  commands:
    commitlint:
      run: bunx --no-install commitlint --edit "{1}"
pre-push:
  commands:
    check:
      run: bun run check
